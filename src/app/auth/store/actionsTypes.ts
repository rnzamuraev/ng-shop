export enum ESignUp {
  SIGN_AP = "[USER] Sign Up",
  SIGN_AP_SUCCESS = "[USER] Sign Up success",
  SIGN_AP_FAILURE = "[USER] Sign Up failure",
}

export enum ESignIn {
  SIGN_IN = "[USER] Sign In",
  SIGN_IN_SUCCESS = "[USER] Sign In success",
  SIGN_IN_FAILURE = "[USER] Sign In failure",
}

export enum ECheckTheEmail {
  CHECK_EMAIL = "[USER] Check email",
  CHECK_EMAIL_SUCCESS = "[USER] Check email success",
  CHECK_EMAIL_FAILURE = "[USER] Check email failure",
}
